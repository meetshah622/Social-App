/* eslint-disable react/prop-types */
import "./NavModuleIconStyle.scss";
import { Button } from "antd";
function NavModuleIcon({ icon }) {
  return <a className="navModuleIconStyle">{icon}</a>;
}

export default NavModuleIcon;
